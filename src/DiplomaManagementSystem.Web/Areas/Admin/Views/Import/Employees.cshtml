@model DiplomaManagementSystem.Web.Areas.Admin.Models.ImportEmployeesViewModel
@{
    ViewData["Title"] = AdminPageTitles.ImportEmployees;
}

<h1 class="mb-4">@ViewData["Title"]</h1>

<partial name="_TempDataAlerts"/>

<partial name="_AdminNavLinks" model="AdminNavigation.EmployeesBack()"/>

<p>Формат: ПІБ, email, вчене звання (опційно), скорочене ПІБ (опційно). Перший рядок — заголовок.</p>

<form asp-action="Employees" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()
    <div class="mb-3">
        <label asp-for="File" class="form-label">Файл (.csv, .xlsx)</label>
        <input asp-for="File" class="form-control" type="file" accept=".csv,.xlsx"/>
        <span asp-validation-for="File" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Імпортувати</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial"/>
}

@if (Model.Result is not null)
{
    <div class="alert alert-info mt-4">
        <strong>Результат:</strong>
        всього @Model.Result.TotalRows,
        імпортовано @Model.Result.ImportedCount,
        пропущено @Model.Result.SkippedCount
    </div>
    @if (Model.Result.Errors.Count > 0)
    {
        <ul class="text-danger">
            @foreach (string error in Model.Result.Errors)
            {
                <li>@error</li>
            }
        </ul>
    }
}
